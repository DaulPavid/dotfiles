# dotfiles

Personal config for bash, tmux, git, and ssh.

## Usage

```
$ ./install.sh 
```

Or run the scripts individually:

```
$ ./install/packages.sh
$ ./install/extras.sh
$ ./link
```

## Tools

- fzf, ripgrep, fd, bat
- eza, zoxide, tree
- jq, yq, git-delta
